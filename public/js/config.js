// Frontend configuration. Set this value in Pages or on the page to point the
// static frontend at a running backend server. Default is empty (same origin).
window.API_BASE = window.API_BASE || '';

// Example: to point to https://example.com, set in HTML before loading scripts:
// <script>window.API_BASE = 'https://example.com'</script>
